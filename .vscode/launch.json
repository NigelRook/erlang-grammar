{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "cwd": "${workspaceRoot}",
            "request": "launch",
            "program": "${workspaceRoot}/tools/tokenize.js",
            "args": [ "${workspaceRoot}/out/erlang.json" ],
            "externalConsole": true,
            "preLaunchTask": "build"
        }
    ]
}